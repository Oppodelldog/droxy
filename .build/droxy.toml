Version="1"

    [[command]]
      name = "basic go command"
      image = "oppodelldog/droxy:latest"
      isTemplate = true
      addGroups = true
      impersonate = true
      workDir = "${PWD}"
      removeContainer=true
      isInteractive=true
      # volume mappings
      volumes = [
          "${HOME}:${HOME}",
          "${GOPATH}:${GOPATH}",
          "/etc/passwd:/etc/passwd:ro",
          "/etc/group:/etc/group:ro",
      ]

      # environment variable mappings
      envvars = [
          "GOPATH=${GOPATH}",
          "HOME=${HOME}",
      ]

#    [[command]]
#        template = "basic go command"
#    	name = "go"
#    	entryPoint = "go"
#    	image = "golang:1.10.0"
#
    [[command]]
        template = "basic go command"
    	name = "make"
    	entryPoint = "make"

    [[command]]
        template = "basic go command"
    	name = "baesch"
    	entryPoint = "bash"